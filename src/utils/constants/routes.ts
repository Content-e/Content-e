export enum UnAuthRoutes {
  Login = "/login",
  Register = "/register",
  Reverify = "/reverify",
  Landing = "/",
}

export enum AuthRoutes {
  Explore = "/explore",
  Home = "/home",
  Brand = "/brand",
  Logout = "/logout",
}
