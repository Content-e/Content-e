export enum UnAuthRoutes {
  Login = "/login",
  Register = "/register",
  Reverify = "/reverify",
  Landing = "/",
}

export enum AuthRoutes {
  Home = "/dashboard",
  Brand = "/brand",
  EditBrand = "/brandEdit",
  Logout = "/logout",
}
