export enum UnAuthRoutes {
  Login = "/login",
  Register = "/register",
  Reverify = "/reverify",
  Landing = "/",
}

export enum AuthRoutes {
  Home = "/home",
  Brand = "/brand",
  Logout = "/logout",
}
